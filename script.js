'use strict';
 
/*
function logger() {
    console.log(`My name is Icaro!`)
}

// Calling / Invoking / Running the function
logger()
logger()
logger()

function fruitProcessor (apples, oranges) {
    const juice = `Juice with ${apples} apples and ${oranges} oranges.`
    return juice
}

const appleJuice = fruitProcessor(5, 0)
console.log(appleJuice)

const appleOrangeJuice = fruitProcessor(2, 4)
console.log(appleOrangeJuice)


const friends = ['john', 'michael', 'sylas', 'peter'];
console.log(friends)
friends.unshift('michael')
console.log(friends)



const icaro = [
    'Icaro',
    'Casaes',
    2021 - 1997,
    'businessman',
    ['Rodrigo', 'Kalec', 'RaVi', 'Icaro', 'Do Gato']
];

const types = [];


for(let i = 0; i < icaro.length; i++) {
    types.push(typeof icaro[i]);
};

console.log(types);

//Crescente
for(let i = 0; i < icaro.length; i++) {
    console.log(icaro[i]);
};

//Decrescente
for(let i = icaro.length - 1; i >= 0; i--) {
    console.log(icaro[i]);
};

console.log('-------------- continue / break ---------------')
//continue and break
for (let i = 0; i < icaro.length; i++) {
    if (typeof icaro[i] !== 'string') continue;


    console.log(icaro[i], typeof icaro[i])
}

console.log('-------------- interrupt at a number ---------------')

for (let i = 0; i < icaro.length; i++) {
    if (typeof icaro[i] === 'number') break;


    console.log(icaro[i], typeof icaro[i]);
}



//loop inside other loop

for (let exercise = 1; exercise < 4; exercise++) {
    console.log(`----------- Starting exercise ${exercise}! -----------`);
    
    for (let rep = 1; rep < 6; rep++) {
        console.log(`Exercise ${exercise}: Lifting weight repetition ${rep}! 🏋🏼‍♂️`);
    }
};


let rep = 1;
while (rep <= 10) {
    console.log(`lifting repetition ${rep}!`);
    rep++;
}

let dice = Math.trunc(Math.random() * 6) + 1;
console.log(dice);

while (dice!== 6) {
    console.log(`You rolled a ${dice}`);
    dice = Math.trunc(Math.random() * 6) + 1;
    if (dice === 6) console.log("You're a lucky mofo");
};
*/

